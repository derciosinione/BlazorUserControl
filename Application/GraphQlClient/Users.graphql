

query GetAllUsers {
    allUsers {
        ...UserFields
    }
}

query GetUserById($id: UUID!) {
    user(id: $id) {
        ...UserFields
    }
}

fragment UserFields on UserType {
    id
    name
    email
    username
    updatedAt
    createdAt
}
